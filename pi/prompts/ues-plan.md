---
description: Produce an implementation-ready UES plan on Pi without editing code.
---

Plan this engineering task without editing files: $@

Inspect repository instructions, relevant entry points, interfaces, callers, tests, nearest working analogues, and constraints. Use `ues_cli` for deterministic repository/evidence commands when useful. For a complex plan, use `ues_dispatch` with `ues-architect` and optionally `ues-plan-checker`.

Define observable acceptance criteria, blast radius, ordered implementation steps, dependencies, file/resource scopes, rollback or compatibility concerns, and verification for each risky boundary. Call out unresolved decisions rather than inventing them.
