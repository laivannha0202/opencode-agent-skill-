---
description: Produce an implementation-ready, evidence-based plan without editing code.
argument-hint: "<task>"
---

Plan this engineering task without editing files: $ARGUMENTS

Inspect relevant repository instructions, entry points, nearest working analogues, interfaces, callers, tests, and constraints. Define observable acceptance criteria, blast radius, ordered implementation steps, compatibility/rollback concerns, task dependencies, and verification for each risky boundary. Use a read-only `ues_fresh_agent` only when fresh isolated analysis would materially improve the plan. Call out unresolved decisions rather than inventing them.
