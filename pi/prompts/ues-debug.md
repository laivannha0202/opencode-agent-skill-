---
description: Investigate a failure and identify the supported root cause before patching.
argument-hint: "<failure>"
---

Debug this problem: $ARGUMENTS

Reproduce or capture the exact failure, collect environment/version clues, trace where state first becomes wrong, compare a nearby working analogue, and form one evidence-backed hypothesis at a time. Do not stack speculative fixes. If a code change is required, keep it minimal and verify the original failure plus affected behavior.
