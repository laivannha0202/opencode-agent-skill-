---
description: Independently verify current work against acceptance criteria using fresh evidence.
argument-hint: "[scope]"
---

Verify this work without editing: $ARGUMENTS

Identify the actual acceptance criteria and original failure/behavior. Run the narrowest relevant project-native checks, expand based on blast radius, inspect real output and exit status, and inspect final diff/status. Prefer a read-only `ues_fresh_agent` when an independent context is useful. Report exactly what passed, failed, was skipped, or remains unproven.
