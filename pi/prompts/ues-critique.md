---
description: Challenge a proposed implementation or plan for hidden assumptions, failure modes and missing evidence.
---

Critique this proposal or implementation: $@

Use `ues_dispatch` with `ues-critic` for independent fresh context when useful. Look for incorrect assumptions, missing dependencies, unsafe ordering, inadequate rollback, weak acceptance criteria, test gaps, and unnecessary complexity. Ground each objection in concrete repository or requirement evidence.

Do not turn critique into an implementation unless asked.
